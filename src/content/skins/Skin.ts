export abstract class Skin {
    /**
     * Display, centered
     * @param skin 
     * @param x 
     * @param y 
     * @param size 
     */
    public abstract display(x: number, y:number, size: number): void;
}